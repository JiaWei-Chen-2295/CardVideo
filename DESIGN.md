# CardVideo 设计规格（经 /grilling 拷问定稿）

> 一个"照片即卡片"的 WebAR 应用：用户上传一段视频 + 一张照片，系统生成分享链接；
> 观看者打开链接、用手机对着**打印出来的那张照片**拍摄，照片上会升起一块 3D 悬浮屏播放视频。

本文档中所有标 **[实测]** 的结论都来自本仓库 `scratch/compile-test/` 下的真实运行结果，
不是推测。标 **[已查证]** 的来自官方文档，附链接。

---

## 1. 产品定义

### 1.1 两个角色

- **创建者 A**：上传视频 + 照片 → 拿到分享链接 → 把链接和照片（打印件）给 B。
- **观看者 B**：打开链接 → 对着打印的照片拍摄 → 照片上浮起 3D 屏幕播放视频。

### 1.2 已锁定的产品决策

| # | 决策 | 结论 | 备注 |
|---|---|---|---|
| P1 | 追踪目标 | **上传的照片本身**，不做通用卡片模式 | 明确不预留通用卡片退路 |
| P2 | 照片传递 | A 把照片发给 B（微信/线下） | 因此 B 必须**打印**照片或用第二块屏幕显示 |
| P3 | 追踪内容 | **单卡单图单视频** | 排除画廊/多卡模式 |
| P4 | 屏幕形态 | **只有升起动画（方案 C）** | 无平面态、无 3D 机身、无画廊 |
| P5 | 位置参数 | **写死，不提供调节面板** | 具体数值见 §4.3 |
| P6 | 声音 | **静音自动播放 + 一个「开启声音」按钮** | 绕过 iOS 自动播放限制 |
| P7 | 退出 | **全屏沉浸 + 右上角一个极小的「×」** | |
| P8 | 开场 | **开场一屏 + 「开始体验」按钮 + 预加载 + 进度提示** | 该次点击顺带满足 iOS 音频手势要求 |
| P9 | 追踪丢失 | 丢失后**保持最后位置 0.8 秒并暂停视频**，超时才反向收起；重识别则续播 | 避免手抖导致屏幕抽搐 |
| P10 | 上传预检 | **素图/低特征图直接拦截**，不让 B 拿到永远扫不出来的卡片 | 判定逻辑见 §5.2 |
| P11 | 视频来源 | **用户自行上传 MP4** | B站 iframe 方案已否决（见 §3） |
| P12 | 验收标准 | A5 彩打、室内照明、后置摄像头、25–45cm 距离：**2 秒内识别成功，10 秒内不闪断（允许 ≤1 次短暂丢失）** | |

---

## 2. 技术选型

| 层 | 选型 | 状态 |
|---|---|---|
| 图像追踪 | **MindAR 1.2.5**（`mind-ar`，MIT） | [已查证] npm latest = 1.2.5 |
| 3D 渲染 | **Three.js**（peer dep，`>=0.136.0`） | [已查证] npm latest = 0.186.0 |
| 追踪编译 | **浏览器端 `Compiler` 类（Web Worker）** | 见 §5.1，Vercel 约束所迫 |
| 后端 | Node（Express 或 Fastify，Vercel 官方零配置支持） | [已查证] |
| 部署 | **Vercel**（用户指定） | |
| 媒体存储 | **任意 S3 兼容对象存储** ✅已定 | 实际部署用**腾讯云 COS**（支付宝可付、新用户 50GB+10GB/月×6 个月）；R2 因需外币卡被替换 |
| 元数据存储 | **与媒体同源的对象存储** ✅已定 | 卡片记录仅几百字节且写入一次，无需数据库；见 §2.2 |
| 前端 | **无构建 + import map** ✅已定 | 只从 `/vendor/` 引预打包产物 |
| 本地 AR 自检 | **做** ✅已定 | A 上传后当场用手机验证照片可被识别 |
| 卡片管理 | **最小删除能力** ✅已定 | 管理链接可删除卡片及其对象存储文件 |

### 2.0 存储适配器（关键设计）

存储层抽象为可切换适配器：本地开发 = 磁盘（零凭证即可跑通全链路），
生产 = 任意 S3 兼容对象存储（COS / 七牛 / OSS / R2 改配置不改代码）。

```
StorageAdapter:  createUploadTicket / readUrl / deleteObject / getObjectText / putObjectText
CardStore:       createCard / getCard / deleteCard
```

### 2.1 为什么从 R2 换成 COS

R2 流量免费，是所有选项里带宽成本最低的——**但它要外币卡**。COS 支持支付宝，
代价是外网下行流量 ≈ ¥0.5/GB（绑 CDN 后回源 ≈ ¥0.15/GB）。

**代价必须被记住**：每次观看都完整下载一遍视频，一个 25MB 视频被看 1000 次就是 25GB ≈ ¥12.5。

### 2.2 为什么元数据不另建数据库

一张卡片只有几百字节，且**写入一次、永不修改**——没有读-改-写竞争，id 在写入前已生成，
没有冲突需要仲裁。数据库能提供的（事务、索引、查询）这里都不需要。

这个选择换来的好处不只是省钱：**只依赖一个服务商、一套凭证、一个免费额度**，而不是两个。
放弃的唯一能力是"列出所有卡片"，应用里没有任何地方需要它。

### 2.1 数据流

```
A 的浏览器
  ├─ 1. 读照片 → 缩放+灰度 → 编译 .mind（Web Worker，3.6~20s，带进度）
  ├─ 2. 【本地 AR 自检】让 A 当场确认这张照片能被扫出来（推荐项，§8-Q4）
  ├─ 3. 请求 /api/upload-ticket → 后端签发 presigned URL
  ├─ 4. 浏览器直传 R2：原始照片 / MP4 / .mind   ← 绕开 Vercel 4.5MB 限制
  └─ 5. POST /api/cards → 后端只写入元数据（id、文件 key、时长、尺寸）

B 的浏览器
  ├─ GET /c/{id} → 落地页（缩略图 + 说明 + 「开始体验」）
  └─ 「开始体验」→ 请求相机权限（与预加载并行）→ 拉取 .mind + MP4
       → MindAR 追踪 → 识别成功 → 升起动画 + 静音播放
```

---

## 3. 被否决的方案（连同否决理由，避免将来重复讨论）

### 3.1 B站 iframe 嵌入 —— ❌ 否决

四条硬伤，任何一条都足以否决：

1. **跨域 iframe 拿不到内部 `<video>`**，无法作为纹理喂给 WebGL/Three.js，"3D 屏幕"物理上做不出来；`drawImage` 进 canvas 会被跨域污染。
2. 嵌入的是**播放器**而非视频：控制条、圆角、UP主头像、弹幕层会被一起贴到 AR 里；且 iframe 是平面矩形，不参与 3D 变换与遮挡。
3. **性能**：AR 追踪（WASM 特征匹配）+ WebGL 渲染 + 跨域 iframe 内完整播放器同时运行，中端安卓会掉到个位数帧率。
4. **iOS Safari 对跨域 iframe 的自动播放限制更严**，典型结果是"扫出来了但屏幕是黑的"。

替代方案（youtube-dl 类服务端抽取）也被否决：**视频来源已定为用户自行上传 MP4**，整条链路不再依赖第三方。

### 3.2 服务端编译 `.mind` —— ❌ 否决（在 Vercel 上）

- `@tensorflow/tfjs` + `tfjs-core` + `tfjs-backend-cpu` + `mind-ar` **[实测]** 未压缩合计 **≈ 200 MB**，
  Vercel 函数上限 250 MB（[已查证](https://vercel.com/docs/functions/limitations)）。贴着悬崖，每次冷启动还要重新加载。
- 改为浏览器编译后，函数体积回到几百 KB，且**顺带实现了"A 上传后立刻 AR 自检"**。
- 保留服务端编译能力作为本地开发/批处理工具（已实测可行，见 §6.2）。

### 3.3 `node:sqlite` + 本地 `data/` 目录 —— ❌ 否决（在 Vercel 上）

Serverless 函数只有 `/tmp`，调用结束即销毁且不跨实例共享；项目目录在 Vercel 上只读。
必须换托管数据库 + 对象存储（§2）。

---

## 4. AR 渲染规格

### 4.1 追踪层

MindAR 的 `MindARThree`：`anchor = mindarThree.addAnchor(0)`。
锚点坐标系中，追踪图占据 `[-w/2, w/2] × [-h/2, h/2]`，其中 `w/h` 为照片宽高比；
屏幕内容挂在 `anchor.group` 下。

### 4.2 升起动画（方案 C，唯一形态）

- 起始态：`z = 0`（完全压平在照片表面）、`rotation.x = -90°`（贴地）、`scale = 0.6`
- 终止态：§4.3
- 时长 **600–800ms**，`ease-out`（cubic）
- 追踪丢失 0.8 秒后播放**反向**动画收起

### 4.3 终态位置参数（写死）

| 参数 | 值 |
|---|---|
| 沿照片法线抬高 | 照片宽度的 **0.25 倍** |
| 立面倾角 | 向观察者倾斜 **18°** |
| 屏幕宽度 | 照片宽度的 **1.2 倍** |
| 屏幕高度 | 按视频宽高比计算 |
| 水平位置 | 居中 |

> 调参入口：这三个值集中定义在一个常量文件里，真机验收后按 §9 微调。

### 4.4 材质

- 屏幕主体：`MeshBasicMaterial` + `VideoTexture`（自发光观感，不受光照影响 → 像发光屏而非纸片）
- 窄边框：一圈极细的 `MeshBasicMaterial` 描边，提供"屏幕边界"的视觉锚定
- 不做 bloom 后处理（性能优先；如真机帧率有富余再加）

### 4.5 追踪状态机（对应 P9）

```
未识别 ──识别到──> 升起中(0.6~0.8s) ──> 展示中(视频静音播放)
                    ↑                        │
                    └──重新识别───────────┐  │追踪丢失
                                        │  ↓
                              收起中 <──0.8s 宽限(视频暂停,位置冻结)
```

### 4.6 相机与全屏

- `facingMode: 'environment'`（后置摄像头）
- iOS Safari **不支持** 元素级 Fullscreen API：用 `viewport-fit=cover` + 固定全视口布局 +
  `apple-mobile-web-app-capable` 达成沉浸效果，**不要依赖 `requestFullscreen()`**
- **必须 HTTPS**（否则相机权限直接被拒；Vercel 默认提供 HTTPS）

---

## 5. 照片预检与编译

### 5.1 编译参数

[实测] 编译耗时与图像长边近似线性：

| 长边 | 预检 | 编译 | `.mind` | 匹配尺度 | 追踪点数 |
|---|---|---|---|---|---|
| 1600px | 173ms | 20135ms | 358KB | 12 | 25/29 |
| 1280px | 280ms | 14539ms | 343KB | 11 | 20/28 |
| 1024px | 266ms | 9751ms | 332KB | 10 | 22/29 |
| 768px | 269ms | 6690ms | 313KB | 9 | 23/27 |
| 512px | 253ms | 3570ms | 260KB | 7 | 18/28 |

**决策：目标图长边降采样到 768–1024px 后编译。** 依据：

- 追踪阶段**硬编码**只用 256px/128px 两档（`buildTrackingImageList`），原始分辨率对识别质量贡献饱和
- [实测] 追踪点数在 512–1600px 全程稳定在 18~29 个，不随尺寸增长
- `.mind` 体积全区间只有 0.26–0.36 MB，加载很快
- 768px 编译 6.7 秒、1024px 编译 9.8 秒，都在可接受的进度提示范围内

### 5.2 素图拦截（P10）

[实测] 预检（追踪特征普查）耗时 **173–280ms**，成本极低，可同步执行。

判定依据必须**包含但不限于**点数：

1. 对两个追踪尺度（minDim 缩到 256px 与 128px）统计特征点数
2. **点数过低** → 拦截（纹理太少）
3. **点数异常高但分布均匀/自相似** → 也要警惕：
   [实测] 一张完美棋盘格纹理图，**追踪点数 = 0**，因为 `extract.js` 里
   `MAX_SIM_THRESH = 0.95` / `SD_THRESH = 8.0` 的自相似度抑制会把"到处都是同一个点"的规整图案全部丢弃。
   → **"有纹理" ≠ "能追踪"：规则重复图案（格子布、规则砖墙、纯几何纹理）反而失败。**
4. 具体阈值：**待用真实照片标定**（见 §9-A1），在标定完成前先用保守阈值 + 明确的失败文案

### 5.3 上传引导文案（必须包含）

- 「请把这张照片**打印出来**」+ 建议 A4 纸彩打、短边 ≥ 10cm
- 光照充足、避免反光
- 距离 25–45cm 对准照片
- 素图会被拒绝，并提示"换一张有细节的照片，或给照片加一圈带纹理的边框"

---

## 6. 后端接口（草案）

### 6.1 HTTP 接口

| 方法 | 路径 | 说明 |
|---|---|---|
| `POST` | `/api/upload-ticket` | 签发 presigned URL（R2）；返回上传地址与最终 key |
| `POST` | `/api/cards` | 写入元数据：`{ photoKey, videoKey, mindKey, videoDuration, aspect, title }` → 返回 `{ id, shareUrl }` |
| `GET` | `/api/cards/:id` | 落地页读取元数据 |
| `GET` | `/c/:id` | 落地页（SSR 或静态壳 + 客户端取数） |
| `DELETE` | `/api/cards/:id` | 删除卡片（待确认，§8-Q5） |

**注意**：任何接口的请求/响应 body 都不得超过 **4.5MB**（[已查证](https://vercel.com/docs/functions/limitations)）。
大文件一律走 presigned URL 直传对象存储，**永不经过函数**。

### 6.2 服务端编译工具（本地保留）

[实测] mind-ar 的 `OfflineCompiler` **可以在没有原生 `canvas` 模块的情况下运行**：
用 ESM resolve hook 把 `canvas` 替换为桩模块，直接喂预缩放+灰度的像素数据。
完整可运行代码见 `scratch/compile-test/`（`canvas-hook.mjs` / `canvas-stub.mjs` / `register-hook.mjs` / `probe-*.mjs`）。
用途：本地 CLI 编译、将来做批处理或兜底。

### 6.3 分享码

- **12 位 base62 随机码**，不可猜（P 待确认 §8-Q5 关于是否加删除能力）

---

## 7. 本机环境约束（[实测]，会反复踩到）

1. **npm 的 lifecycle script 被禁止执行**（沙箱 `EPERM spawn`）→ 任何需要 postinstall 下载原生二进制的包
   （`canvas`、`sharp` 等）**装不上**。这正是 §6.2 桩模块方案的成因。
2. **npm 默认缓存目录在工作区外** → 必须加 `--cache D:\a_my_project\CardVideo\.npm-cache`
3. **nvm 的 `npm.ps1` 包装脚本在沙箱内失败**（`StandardOutputEncoding`）→
   需直接调用 `node <npm-cli.js>`；且在 PowerShell 里**不能接管道**（`| Select-Object` 会触发 EPERM），
   但可用 `cmd /c "..."` 包装。
4. **`web_fetch` 无法访问 vercel.com / github.io 等域名**（DNS 解析到非公网 IP）→
   改用 anysearch 的 `extract` 命令读取这些页面（已验证可行）。
5. Node **v24.21.0**，npm 11.19.0。

---

## 8. 已确认的收尾决策（全部按推荐锁定）

| # | 问题 | 结论 |
|---|---|---|
| Q1 | 前端形态 | **无构建 + import map** |
| Q2 | 媒体存储 | ~~Cloudflare R2~~ → **腾讯云 COS**（R2 需外币卡，见 §2.1） |
| Q3 | 元数据库 | ~~Turso libSQL~~ → **与媒体同源的对象存储**（见 §2.2） |
| Q4 | 本地 AR 自检 | **做** |
| Q5 | 卡片管理 | **加最小删除能力** |

**部署前需要用户提供（5 个环境变量，全是腾讯云一家的）**：
`S3_ENDPOINT` / `S3_REGION` / `S3_ACCESS_KEY_ID` / `S3_SECRET_ACCESS_KEY` / `S3_BUCKET`，
外加可选的 `S3_PUBLIC_BASE_URL`（绑定到桶的域名，强烈建议，能显著降低流量成本）。

---

## 10. 实现记录（与设计稿的差异）

落地时基于实测做了以下调整，均已写入代码注释：

1. **编译尺寸定为长边 1024px**（`public/js/ar/mind-compile.worker.js`）。设计稿只写了
   "768–1024 区间"，实测后取上界：编译约 9.8 秒，比 1600px 省一半时间，同时保留 10 级匹配
   尺度（比 768px 的 9 级更稳）。
2. **预检不再单独统计特征点，而是复用编译产物。** 最初计划"编译前跑一次特征普查"，但
   `extract.js` 虽然零依赖，跑完整编译仍需加载 `@tensorflow/tfjs`。最终做成两层：
   ① 上传页先用 vendored 的 `extract.js` 做 ~200ms 快速预检（零额外依赖）；
   ② 编译完成后再用产物里的 `trackingData` 做权威判定，任一尺度特征点 < 8 则拒绝且不上传。
3. **服务端编译路径未做成 CLI。** 设计稿 §6.2 说"本地保留"。实际未落地为脚本，因为浏览器
   编译已成为唯一路径且无需服务端兜底；`canvas` 桩模块的可行性验证保留在
   `scratch/compile-test/`（已被 .gitignore 排除）。
4. **新增了 `public/debug.html`。** 无构建前端最容易静默失败（import map 解析、Worker 启动、
   编译产物可解析性），所以加了一个浏览器内自检页，部署后第一件事就是打开它。
5. **新增 `compression` 中间件。** AR 页要加载 three.js + mind-ar 渲染包共约 4MB 未压缩 JS，
   手机流量下必须 gzip。
6. **自检数据只保留 6 小时并在读取后立即删除。** 一条记录会钉住整个视频 Blob，不清理会静默
   吃掉用户几百 MB 浏览器存储。
7. **新增物料落地页 `/m/:id`（规格与提示词见 `MATERIAL.md`）。** 产品上这不是新功能，而是
   **换了一个读者**：`/c/<id>` 的读者收到一张照片，必须被告知"打印出来"；`/m/<id>` 的读者
   手上已经拿着印刷件，同一句话就从提示变成了噪音（会让人怀疑自己漏了一步）。所以 `/m/` 只留
   画面、「把镜头对准卡片」和一个按钮，全页可见文字 43 个字；AR 侧用 `theme=material` 切到
   对应文案。**只切文案，不切机制**——iOS 音频手势、相机权限时机、并行预加载、0.8 秒追踪宽限
   全部与通用版共用，那些决定"能不能用"，不是"好不好看"。物料版额外预热 `.mind`（约 300KB，
   是启动路径上唯一必须下完才能开始的资源）；视频刻意不预热，几十 MB 不该替用户决定花掉。
8. **`check:frontend` 与 `check:entrypoints` 都补上了 `/m/` 的断言。** 这两个检查原来只覆盖
   `/c/`。§3 里那次"部署成功、页面全部正常，但整个 API 从未被调用"的坑，以及 `cleanUrls`
   让 `.html` 重写静默失效的坑，都是**没有报错但结果是错的**——不被断言守着的新路由会把它们
   原样复发一次。
9. **物料版 AR 多了一层取景框浮层（`public/frames/viewfinder.webp`）。** 生成图铺在实时摄像头
   画面之上、所有控件之下（z-index 5 < 10），**中间那块纯色必须抠成透明**，否则不透明的底色会
   把摄像头画面整个盖住。两处值得记下来：
   - **不能整块矩形抠**。四角白线是画在窗口**内部**的，整块抠会把它们一起删掉——而白线恰恰是
     最"取景框"的元素，本来就该浮在实时画面上。最终用"只在窗口矩形内做颜色抠像"：
     底色透明、白线保留、矩形外一律不透明（避免把边框自身的暗部也抠出洞）。
   - **素材必须是竖版**。手机是竖屏，横版素材 `cover` 铺满时左右各裁掉约 15.3%，外框会退化成
     只剩上下两条。所以左右边带必须宽于这个裁切量（取 22%），`scripts/frame-alpha.mjs` 会把
     这个预测和"边带会被裁光"的警告直接打出来。
   代价说明：升起的视频屏渲染在 stage 的 canvas 里，会从取景框边带**下面**穿过。这是刻意的
   ——真取景框就是前景压住画面边缘——但它是这套设计的固有取舍，不是可以"修掉"的问题。

---

## 11. 验收与后续动作

### 11.1 必须由人在真机上完成（我无法代做）

- **B1 真机跑通**：iPhone Safari + 一台安卓 Chrome。打开 `/debug.html` 确认 6 项全绿，
  再走一遍 `/selfcheck`。**这是唯一能验证 AR 效果的方式**——本机无法调用摄像头。
- **B2 位置参数微调**：看到真实画面后调 `public/js/ar/video-screen.js` 里的 `POSE`
  （`liftFactor` / `riseFactor` / `tiltDegrees` / `widthFactor`）。
- **B3 素图阈值标定**：收集 10–20 张真实照片（人脸特写、风景、带文字海报、纯色背景），
  跑预检，标定"可识别/不可识别"的分界，再定 `MIN_TRACKING_POINTS`。目前的值 8 来自合成图，
  **不是真实照片**。
- **B4 降采样验证**：对比 768px vs 1024px 产物在真机上的识别距离与稳定性，确认 10 级匹配
  尺度够用（**这是降采样唯一的真实风险点**）。

